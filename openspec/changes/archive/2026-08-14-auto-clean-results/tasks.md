## 1. Makefile Updates

- [ ] 1.1 Update `ESP32_Auth_PIO/Makefile` to make the `benchmark` target depend on `clean-results`.
- [ ] 1.2 Update the `benchmark-paper-time` target to depend on `clean-results` as well.
